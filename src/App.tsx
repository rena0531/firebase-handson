import React from "react";

interface Comment {
  id: number;
  user: string;
  body: string;
  time: string;
}

interface Form {
  user: string;
  body: string;
}

const App: React.FC = () => {
  return <>Hello</>;
};

export default App;
